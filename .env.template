# Django Environment Variables
# Copy this file to .env and fill in the appropriate values

# Set to False in production
DEBUG=True

# Generate a secure SECRET_KEY using: python -c "import secrets; print(secrets.token_urlsafe(50))"
# CRITICAL: This MUST be set in production with a randomly generated key
SECRET_KEY=your-secret-key-here

# Allowed hosts (comma-separated, required in production)
ALLOWED_HOSTS=localhost,127.0.0.1

# ==============================================================================
# Database Configuration
# ==============================================================================

# For local development with PostgreSQL (optional, falls back to SQLite):
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# For Railway deployment:
# - DATABASE_URL is automatically provided by Railway (PostgreSQL)
# - No need to set this manually in Railway

# To create local PostgreSQL database:
# 1. Install PostgreSQL and ensure it's running
# 2. Create database: `createdb grooming_salon` (or your preferred name)
# 3. Set DATABASE_URL: `postgresql://postgres:password@localhost:5432/grooming_salon`
# 4. Run: `python manage.py migrate`

# ==============================================================================
# Email Configuration (Production)
# ==============================================================================

# Django-Anymail provides Django email backends for multiple email providers
# Supported providers: sendgrid, mailgun, ses, postmark, sparkpost, etc.
EMAIL_BACKEND=anymail.backends.sendgrid.EmailBackend

# Default sender email address
DEFAULT_FROM_EMAIL=noreply@shampooches.com
SERVER_EMAIL=admin@shampooches.com

# SendGrid Configuration (recommended for production)
# Get API key from https://sendgrid.com
SENDGRID_API_KEY=your-sendgrid-api-key-here

# Alternative: Mailgun Configuration
# Get API key from https://mailgun.com
# MAILGUN_API_KEY=your-mailgun-api-key-here
# MAILGUN_SENDER_DOMAIN=your-domain-here

# For development/testing, you can use the console backend:
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# ==============================================================================
# Redis Cache (Optional)
# ==============================================================================

# For production caching with Redis (optional, only if REDIS_URL is provided):
# For Railway: Add Redis service and REDIS_URL is auto-provided
# For local dev: `REDIS_URL=redis://localhost:6379/0`
